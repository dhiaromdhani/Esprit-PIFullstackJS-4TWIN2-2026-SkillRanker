apiVersion: v1
kind: Service
metadata:
  name: skillranker-backend
  namespace: skillranker
spec:
  type: NodePort
  selector:
    app: skillranker-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000
